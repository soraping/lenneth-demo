{
  "dependencies": {
    "koa-logger": "^3.2.0",
    "koa-multer": "^1.0.2",
    "lenneth": "0.2.5",
    "multer": "^1.3.1",
    "request": "^2.87.0",
    "request-promise": "^4.2.2"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build": "yarn clean && tsc"
  },
  "devDependencies": {
    "@types/multer": "^1.3.7",
    "@types/request-promise": "^4.1.42"
  }
}
